Run runtime/post.RUN.md. State: state/. Log: logs/posts.json. No other files.

For You hint: copy-link (20) and mutual replies (20) beat likes (0.5). Insight/take = pasteable list or named recipe. On take (Thu), ecosystem (Sat), and recap (Sun) — end with a short open question that invites builders to reply (e.g. "what's the smallest control you regret not exposing?"). Keep it natural, not bait. On clip (Mon), spotlight (Wed), before_after (Fri) — no question, just the observation + demo if it exists. Self-reply listing links are for buyers, not ranking.
