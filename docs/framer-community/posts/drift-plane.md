# Drift Plane — Community post

**Component:** Drift Plane
**Preview:** https://framer.link/DV7ph9v
**Category:** Gallery
**Marketplace slug:** `drift-plane`
**In marketplace:** yes

## Hook

`Drift Plane — an infinite image plane you actually fly through`

## Body

```
Most Framer galleries stop at a grid or carousel. Drift Plane puts cards on a toroidal 3D plane — idle drift, mouse parallax, three depth layers — so pan never hits an edge.

→ infinite toroidal wrap (no hard edges)
→ idle drift + mouse parallax
→ far / mid / near depth layers
→ optional per-card links (click vs pan)
→ Appear intro + Group Click Exit (toggleable)

No Auto Demo — pan and click are the product. Reduced-motion + canvas/export safe. Zero external packages.

Try it live: https://framer.link/DV7ph9v

[attach video: idle drift → mouse parallax → drag pan wrap → click linked card + group exit]

BuiltByKern → Marketplace → Drift Plane

Portfolio void, moodboard, or lookbook — where would you drop an infinite plane?
```

## Pin comment

(Add canvas vs publish caveat if motion only runs on live site.)
