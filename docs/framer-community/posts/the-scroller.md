# TheScroller — Community post

**Component:** TheScroller
**Preview:** (add framer.link)
**Category:** Carousels
**Marketplace slug:** `kern-thescroller`
**In marketplace:** yes

## Hook

`TheScroller — 3D slider on a real spring scroll engine`

## Body

```
Native carousels lack velocity response. TheScroller runs spring physics over wheel, touch, and keyboard — three visual presets with optional motion blur tied to scroll speed.

→ Distortion — velocity-driven scale
→ Carousel — orbital arc with depth falloff
→ Cards — tilted stack with organic rotation
→ GPU-accelerated, zero layout shift

Per-breakpoint controls. Full keyboard and screen-reader accessibility.

[attach video: scroll distortion preset → carousel arc → cards stack tilt]

BuiltByKern → Marketplace → TheScroller

Distortion, Carousel, or Cards — which 3D preset would you ship?
```

## Pin comment

(Add canvas vs publish caveat if motion only runs on live site.)
