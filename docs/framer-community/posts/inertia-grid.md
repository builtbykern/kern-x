# InertiaGrid — Community post

**Component:** InertiaGrid
**Preview:** (add framer.link)
**Category:** Gallery
**Marketplace slug:** `inertiagrid`
**In marketplace:** yes

## Hook

`InertiaGrid — a media grid that reacts to cursor distance`

## Body

```
Gallery grids look premium in screenshots. On hover they usually just scale. InertiaGrid gives each tile spring-based motion from cursor proximity — drift, repel, or glitch presets.

→ Drift, Repel, Glitch interaction presets
→ per-card spring motion, rotation, and scale
→ responsive columns, gaps, and breakpoints
→ reduced-motion fallback + canvas guard

Tune content, entrance, layout, and background from the panel. Zero external dependencies.

[attach video: cursor across grid → repel preset → drift preset → glitch micro-scatter]

BuiltByKern → Marketplace → InertiaGrid

Editorial drift or bold glitch — which preset fits your layout?
```

## Pin comment

(Add canvas vs publish caveat if motion only runs on live site.)
